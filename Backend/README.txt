You will need to install latest version of 

	Python: https://www.python.org/downloads/

After installing python, open backend folder and open up a terminal

The required packages used in the backend project are specified in the requirements.txt file inside the root directory.  you can simply run the following command with your terminal at the root directory of this project:

	pip install -r requirements.txt
